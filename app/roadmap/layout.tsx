import type { Metadata } from "next";
export const metadata: Metadata = {
  title: "Development Roadmap — Ignara AI",
  description: "A transparent view of Ignara AI's development roadmap — current research, near-term goals, and long-term vision.",
};
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
